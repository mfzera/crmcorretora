# propostas

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build propostas` to build the library.

## Running unit tests

Run `nx test propostas` to execute the unit tests via [Jest](https://jestjs.io).
