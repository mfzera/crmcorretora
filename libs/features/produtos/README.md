# produtos

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build produtos` to build the library.

## Running unit tests

Run `nx test produtos` to execute the unit tests via [Jest](https://jestjs.io).
