# cotacoes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cotacoes` to build the library.

## Running unit tests

Run `nx test cotacoes` to execute the unit tests via [Jest](https://jestjs.io).
