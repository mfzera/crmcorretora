# usuarios

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build usuarios` to build the library.

## Running unit tests

Run `nx test usuarios` to execute the unit tests via [Jest](https://jestjs.io).
