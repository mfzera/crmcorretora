# seguradora

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build seguradora` to build the library.

## Running unit tests

Run `nx test seguradora` to execute the unit tests via [Jest](https://jestjs.io).
