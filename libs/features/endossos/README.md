# endossos

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build endossos` to build the library.

## Running unit tests

Run `nx test endossos` to execute the unit tests via [Jest](https://jestjs.io).
