# renovacoes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build renovacoes` to build the library.

## Running unit tests

Run `nx test renovacoes` to execute the unit tests via [Jest](https://jestjs.io).
