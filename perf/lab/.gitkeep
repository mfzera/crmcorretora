# Artefatos de lab (Lighthouse JSON, stats.html, React Profiler).
# Gere com os comandos do ../README.md. Commit opcional — se ficarem pesados,
# adicione `perf/lab/*` ao .gitignore e guarde fora do repo.
