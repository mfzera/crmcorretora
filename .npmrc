# Enable hoisting for better compatibility
shamefully-hoist=true

# Auto-approve build scripts for known dependencies
enable-pre-post-scripts=true

# Allow scripts for specific packages that need them
# This prevents the build script warning in Vercel
unsafe-perm=true

# Increase network timeout for CI/Docker builds
fetch-timeout=120000
